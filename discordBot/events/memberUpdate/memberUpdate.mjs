export async function memberUpdateEventHandler(oldMember, newMember) {

    // TODO Check if member has new roles, and check if the new one is Tributed to me :3

    console.log(oldMember, newMember);
}